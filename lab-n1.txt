#include <stdio.h>

int main() {
    int a = 1;
    int b = 2;
    int c = 3;
    
    int V, S;

    V = a * b * c; 
    S = 2 * (a * b + b * c + a * c);

    printf("Вхідні дані: a = %d, b = %d, c = %d\n", a, b, c);
    printf("Вихідні дані: V = %d, S = %d\n", V, S);

    return 0;
}